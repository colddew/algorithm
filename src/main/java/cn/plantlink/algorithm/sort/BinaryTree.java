package cn.plantlink.algorithm.sort;

/**
 * 二叉树
 */
public class BinaryTree {

    // 只有一个节点则深度为1；只有左子树则深度为左子树深度加1；只有右子树则深度为右子树深度加1；左右子树都存在则取较大值加1
    public static int depth() {
        return 0;
    }

    // 广度优先遍历
    public static int width() {
        return 0;
    }
}
